/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {numero} from './numero';

describe('numero', () => {
  it('should create an instance', () => {
    expect(new numero()).toBeTruthy();
  });
});
